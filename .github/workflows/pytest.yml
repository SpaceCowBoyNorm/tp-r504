name: tests unitaires
on: [push]
jobs:
  test1:
    runns-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python for Demo
        uses: actions/setup-python@v4
      - name: Run tests
        run: pystest TP1/test.py
